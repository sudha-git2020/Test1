pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'CMake'
            }
        }
        stage('Test') {
            steps {
                sh 'CMake test'
            }
        }
    }
}
