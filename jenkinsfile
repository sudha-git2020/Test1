pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'CMake_MyInstall'
            }
        }
        stage('Test') {
            steps {
                sh 'CMake_MyInstall test'
            }
        }
    }
}
